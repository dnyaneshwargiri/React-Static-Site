import * as React from 'react';
export default function ReactFacts() {
  return (
    <div>
      <ul>
        <li>Time for the UI development is faster in react. </li>
        <li>React is not termed as framework it is termed as JavaScript library.</li>
        <li>Virtual DOM is faster than real DOM.</li>
        <li>React devlopment & SEO goes side by side.</li>
      </ul>
    </div>
  );
}
